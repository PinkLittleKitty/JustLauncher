<Window x:Class="JustLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JustLauncher"
        mc:Ignorable="d"
        Title="JustLauncher" Height="800" Width="900"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#00C851"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00A041"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#008A31"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2196F3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border Background="#1A1A1A" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10" Opacity="0.3" BlurRadius="20"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#0D1117" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
  
                        <TextBlock Text="JustLauncher" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="−" Width="30" Height="30" Style="{StaticResource ModernButton}" 
                                Click="MinimizeButton_Click" FontSize="16" Padding="0"/>
                        <Button Content="✕" Width="30" Height="30" Style="{StaticResource ModernButton}" 
                                Click="CloseButton_Click" FontSize="12" Padding="0" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Settings -->
                <Border Grid.Column="0" Background="#21262D" CornerRadius="12" Padding="25">
                    <StackPanel>
                        <TextBlock Text="Launch Settings" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,25"/>
                        
                        <!-- Profile Selection -->
                        <TextBlock Text="Profile" Foreground="#8B949E" FontSize="12" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="ProfileComboBox" Grid.Column="0" Style="{StaticResource ModernComboBox}" 
                                      SelectionChanged="ProfileComboBox_SelectionChanged"/>
                            <Button x:Name="NewProfileButton" Grid.Column="2" Content="+" Width="30" Height="34" 
                                    Style="{StaticResource PrimaryButton}" Click="NewProfileButton_Click" FontSize="16" Padding="0"/>
                            <Button x:Name="EditProfileButton" Grid.Column="4" Content="✏️" Width="30" Height="34" 
                                    Style="{StaticResource ModernButton}" Click="EditProfileButton_Click" FontSize="12" Padding="0"/>
                            <Button x:Name="DeleteProfileButton" Grid.Column="6" Content="🗑️" Width="30" Height="34" 
                                    Style="{StaticResource ModernButton}" Click="DeleteProfileButton_Click" FontSize="12" Padding="0"/>
                        </Grid>
                        
                        <TextBlock Text="Username" Foreground="#8B949E" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="UsernameTextBox" Text="Player" Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Version" Foreground="#8B949E" FontSize="12" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox x:Name="VersionComboBox" Grid.Column="0" Style="{StaticResource ModernComboBox}"/>
                            <Button x:Name="RefreshVersionsButton" Grid.Column="2" Content="↻" Width="40" Height="34" 
                                    Style="{StaticResource SecondaryButton}" Click="RefreshVersionsButton_Click" FontSize="16"/>
                        </Grid>
                        
                        <TextBlock Text="Memory (MB)" Foreground="#8B949E" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="MemoryTextBox" Text="4096" Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Game Directory" Foreground="#8B949E" FontSize="12" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="GameDirTextBox" Grid.Column="0" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                            <Button x:Name="BrowseButton" Grid.Column="2" Content="📁" Width="40" Height="34" 
                                    Style="{StaticResource ModernButton}" Click="BrowseButton_Click" FontSize="12"/>
                        </Grid>
                        
                        <CheckBox x:Name="OfflineModeCheckBox" Content="Offline Mode" Foreground="White" 
                                  IsChecked="True" Margin="0,0,0,30" FontSize="14"/>
                        
                        <Button x:Name="LaunchButton" Content="🚀 Launch Minecraft" Height="50" 
                                Style="{StaticResource PrimaryButton}" Click="LaunchButton_Click" FontSize="16"/>
                        
                        <Button x:Name="DownloadButton" Content="📥 Download Versions" Height="40" Margin="0,10,0,0"
                                Style="{StaticResource SecondaryButton}" Click="DownloadButton_Click" FontSize="14"/>
                    </StackPanel>
                </Border>

                <!-- Right Panel - Console & Downloads -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="250"/>
                    </Grid.RowDefinitions>

                    <!-- Console -->
                    <Border Grid.Row="0" Background="#21262D" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Console Output" FontSize="16" FontWeight="Bold" 
                                       Foreground="White" Margin="0,0,0,15"/>
                            
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <TextBox x:Name="LogTextBox" Background="Transparent" Foreground="#8B949E" 
                                         BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap"
                                         Text="Ready to launch Minecraft..." FontFamily="Consolas" FontSize="12"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Download Panel -->
                    <Border Grid.Row="2" Background="#21262D" CornerRadius="12" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Available Downloads" FontSize="16" FontWeight="Bold" 
                                       Foreground="White" Margin="0,0,0,10"/>
                            
                            <!-- Version Type Filters -->
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                                <CheckBox x:Name="ShowReleasesCheckBox" Content="Releases" Foreground="White" 
                                          IsChecked="True" Margin="0,0,15,0" FontSize="12" 
                                          Checked="FilterCheckBox_Changed" Unchecked="FilterCheckBox_Changed"/>
                                <CheckBox x:Name="ShowSnapshotsCheckBox" Content="Snapshots" Foreground="White" 
                                          IsChecked="False" Margin="0,0,15,0" FontSize="12"
                                          Checked="FilterCheckBox_Changed" Unchecked="FilterCheckBox_Changed"/>
                                <CheckBox x:Name="ShowBetaCheckBox" Content="Beta/Alpha" Foreground="White" 
                                          IsChecked="False" FontSize="12"
                                          Checked="FilterCheckBox_Changed" Unchecked="FilterCheckBox_Changed"/>
                            </StackPanel>
                            
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                <ListBox x:Name="DownloadListBox" Background="Transparent" BorderThickness="0"
                                         Foreground="White" SelectionChanged="DownloadListBox_SelectionChanged">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Padding" Value="10,5"/>
                                            <Setter Property="Margin" Value="0,2"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#2A2A2A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#2196F3"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </ScrollViewer>
                            
                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                <Button x:Name="DownloadSelectedButton" Content="Download Selected" 
                                        Style="{StaticResource PrimaryButton}" Click="DownloadSelectedButton_Click"
                                        IsEnabled="False" Margin="0,0,10,0"/>
                                <ProgressBar x:Name="DownloadProgressBar" Width="150" Height="20" 
                                             Visibility="Collapsed" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
